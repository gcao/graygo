[project]
name = "graygo"
version = "0.1.0"
description = "Gray Go self-play training implementation"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.3",
    "pybind11>=2.13.6",
    "torch>=2.10.0",
]
